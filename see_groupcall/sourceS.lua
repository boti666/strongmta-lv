addEvent("callTaxi", true)
addEventHandler("callTaxi", getRootElement(),
	function (phoneNumber)

	end
)

addEvent("callAmbulance", true)
addEventHandler("callAmbulance", getRootElement(),
	function (phoneNumber)
		
	end
)

addEvent("callPolice", true)
addEventHandler("callPolice", getRootElement(),
	function (phoneNumber)
		
	end
)

addEvent("reportPolice", true)
addEventHandler("reportPolice", getRootElement(),
	function (phoneNumber, reportText)
		
	end
)